# =============================================
# DEVELOPMENT PROFILE CONFIGURATION
# =============================================

spring:
  config:
    activate:
      on-profile: dev

  # MongoDB Atlas for development
  data:
    mongodb:
      uri: mongodb+srv://sachith24bcs10403_db_user:expense-tracker-pass@expense-tracker-cluster.ikio4ur.mongodb.net/expense_tracker_dev?retryWrites=true&w=majority&appName=expense-tracker-cluster
      auto-index-creation: true

logging:
  level:
    com.expensetracker: DEBUG
    org.springframework.data.mongodb: DEBUG
